"use client"; import {use,useState} from "react"; import {API_URL} from "../../../lib";
export default function Correction({params}:{params:Promise<{profileId:string}>}){const {profileId:id}=use(params);const [m,setM]=useState("6000");async function correct(){const r=await fetch(`${API_URL}/profiles/${id}/corrections`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({fieldId:"annual_mileage",value:Number(m)})});if(!r.ok)throw new Error(await r.text());location.href=`/profile/${id}/review`};return <main><h1>Factual correction</h1><label>Corrected annual mileage<input aria-label="Corrected annual mileage" value={m} onChange={e=>setM(e.target.value)}/></label><button onClick={correct}>Create corrected version</button></main>}
