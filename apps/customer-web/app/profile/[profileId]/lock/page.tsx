"use client"; import {use,useState} from "react"; import {API_URL,adminUrl} from "../../../lib";
export default function Lock({params}:{params:Promise<{profileId:string}>}){const {profileId:id}=use(params);const [confirmed,setConfirmed]=useState(false);async function lock(){const r=await fetch(`${API_URL}/profiles/${id}/lock`,{method:"POST"});if(!r.ok)throw new Error(await r.text());location.href=adminUrl(`/admin/profiles/${id}`)};return <main style={{maxWidth:760,margin:"48px auto",padding:24,fontFamily:"system-ui"}}><p>Customer · C-07 · {id}</p><h1>Confirm and lock profile</h1><p>After locking, optimisation can change choices only — not these facts.</p><label><input aria-label="I confirm" type="checkbox" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)}/> I confirm this synthetic factual profile.</label><br/><br/><button disabled={!confirmed} onClick={lock}>Confirm &amp; lock profile</button></main>}
