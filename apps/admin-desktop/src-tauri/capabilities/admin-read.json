{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "admin-read",
  "description": "DB-G3 main-window capability. Only the approved MIQOS read/runtime commands are granted.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "allow-get-runtime-profile",
    "allow-get-health",
    "allow-load-admin-profile-audit"
  ]
}
