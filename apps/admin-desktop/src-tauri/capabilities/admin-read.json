{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "admin-read",
  "description": "DB-G4 main-window capability. Only the approved MIQOS runtime and read-only Admin evidence commands are granted.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "allow-get-runtime-profile",
    "allow-get-health",
    "allow-load-admin-profile",
    "allow-load-admin-profile-version",
    "allow-load-admin-profile-audit"
  ]
}
