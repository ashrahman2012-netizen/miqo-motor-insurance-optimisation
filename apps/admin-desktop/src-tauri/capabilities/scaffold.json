{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "scaffold",
  "description": "G8 main-window capability. Only the three MIQOS proof commands are granted.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "allow-get-runtime-profile",
    "allow-get-health",
    "allow-load-admin-profile-audit"
  ]
}
